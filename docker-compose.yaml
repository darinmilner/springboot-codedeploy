version: "3.9"

services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=reservation'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=user'
    ports:
      - '3306:3306'
